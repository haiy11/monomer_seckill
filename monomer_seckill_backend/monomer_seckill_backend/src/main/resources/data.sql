-- ============================================================
-- 初始化种子数据说明
--
-- 默认账号（admin / user1 / merchant1）与示例商品、秒杀商品
-- 统一由应用启动时的 DataInitializer 以 Java 代码创建（密码在代码内做 MD5，
-- 且商品/秒杀商品需要关联商家用户ID，避免在 SQL 中硬编码主键）。
-- 本文件保留为空，仅作占位。
-- ============================================================
SELECT 1;
